package abstractdemo;

public abstract class RBIBankService {
    // abstract class can have concrete methods & abstract methods.
    // Abstract method syntax:Access specifier name & abstract & return type & class name (); .
    // concrete method syntax : Access specifier name & return type & class name (){ return "message"; }.
    //similar logic but use multiple process ---- concrete method.
    //same process but different logics ------ Abstract method.
    // java will create errors in child class ,only methods in main class.
    // when we go Abstract method - re-usability & templates & implement the inheritance.

}
